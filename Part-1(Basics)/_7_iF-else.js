// *** Decisons making with if-else

const age = 19;

if ( age === 80) {
    console.log(`No way to drive`);
} else if (age > 18) {
    console.log(`Can Drive!`);
} else {
    console.log(`Can't Drive!`);
}