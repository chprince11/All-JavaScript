// Allows to write a secure JS code
'use strict'; // strict mode

let hasDriverLicense = false;
const passTest = true;

if (passTest) hasDriveLicense = true;
if (hasDriverLicense) console.log("I can drive :D");

// const if = "Prince";